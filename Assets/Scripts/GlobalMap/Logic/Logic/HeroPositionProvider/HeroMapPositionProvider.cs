namespace GlobalMap
{
    using UnityEngine;

    public class HeroMapPositionProvider : IHeroMapPositionProvider
    {
        public Vector3 GetHeroPosition()
        {
            //Get Save or new game
            //Where hero was?
            //moving? How much time has passed -> where should he be now
            //In region? Which region?

            return Vector3.zero;
        }
    }
}